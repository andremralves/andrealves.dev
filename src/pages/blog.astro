---
import Posts from '../components/Posts'
import Layout from '../layouts/Layout.astro'

let posts = await Astro.glob('../pages/blog/*.md')
posts = posts.sort(
  (p1, p2) =>
    Date.parse(p2.frontmatter.pubDate) - Date.parse(p1.frontmatter.pubDate)
)
---

<Layout
  title="Blog - André Alves"
  description="The blog section of André Alves's website."
>
  <Posts title="Blog" posts={posts} />
</Layout>
